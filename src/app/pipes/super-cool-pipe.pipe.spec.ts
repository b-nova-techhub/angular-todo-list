import { SuperCoolPipePipe } from './super-cool-pipe.pipe';

describe('SuperCoolPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SuperCoolPipePipe();
    expect(pipe).toBeTruthy();
  });
});
